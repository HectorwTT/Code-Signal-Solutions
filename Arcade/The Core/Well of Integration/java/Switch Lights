int[] solution(int[] a) {
 for(int skipover=0;skipover<a.length;skipover++){
        if(a[skipover]==1){
             for(int i=0;i<=skipover;i++){
                 a[i]=Math.abs(a[i]-1);
             }
        }
    }
    return a;
}
