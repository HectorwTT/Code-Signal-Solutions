boolean solution(int[] a, int[] b, int v) {
   Arrays.sort(a);
   Arrays.sort(b);
   for(int i=0;i<a.length;i++){
       int val1=a[i];
       int val2 = v-val1;
       for(int j=0;j<b.length;j++)
       if(b[j]==val2)return true;
    }
   return false;
}
